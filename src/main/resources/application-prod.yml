# 线上配置文件
# @author heart
# @from heart000_1
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yubi
    username: root
    password: root
    # session 失效时间
  session:
    timeout: 86400